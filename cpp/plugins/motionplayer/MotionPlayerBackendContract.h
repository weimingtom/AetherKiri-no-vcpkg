#pragma once

// Increment this whenever the script-facing registration surface or the
// native backend contract changes. A checked-out private package must declare
// the same version or CMake stops instead of silently producing a fallback or
// partially compatible internal build.
#define AETHERKIRI_MOTIONPLAYER_BACKEND_API_VERSION 3
