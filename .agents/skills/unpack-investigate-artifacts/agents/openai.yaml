interface:
  display_name: "Investigate Diagnostic Artifacts"
  short_description: "Safely unpack archives and trace evidence boundaries"
  default_prompt: "Use $unpack-investigate-artifacts to safely unpack this existing archive or diagnostic package and report evidence-backed findings without running collection."
